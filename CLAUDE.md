# Claude Code instructions

## Git workflow

Always run `git pull --rebase` before pushing. A GitHub Actions workflow auto-commits README updates when plant files change, so the remote may have new commits after a push.
